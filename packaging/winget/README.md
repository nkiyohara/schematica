# WinGet Templates

These manifests are placeholders for future Windows distribution. Schematica
does not publish official Windows binaries while code signing is pending, so the
WinGet package should not be submitted yet.

When Windows signing is ready, update the version, installer URL, and SHA256
fields, then submit the signed installer through the normal WinGet flow.
